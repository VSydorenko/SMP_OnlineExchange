
&НаСервере
Процедура ОбработатьНаСервере(Данные)
	Для Каждого Строка Из Данные Цикл
		СМП_ОнлайнОбменУправлениеОчередями.ОбработкаОчередиДанных(Строка.УникальныйИдентификатор)
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура Обработать(Команда)
	Данные = Новый Массив;
	Для Каждого СтрокаСписка Из Элементы.Список.ВыделенныеСтроки Цикл
		Данные.Добавить(СтрокаСписка);
	КонецЦикла;
	ОбработатьНаСервере(Данные);
КонецПроцедуры
