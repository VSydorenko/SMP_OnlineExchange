
&НаСервере
Процедура ТестFTPНаСервере()
	
	РеквизитФормыВЗначение("Объект").ОтправитьПоFTP(Объект.АдресСервера, Данные);
	
	РеквизитФормыВЗначение("Объект").ПолучитьПоFTP(Объект.АдресСервера);
	
КонецПроцедуры

&НаКлиенте
Процедура ТестFTP(Команда)
	ТестFTPНаСервере();
КонецПроцедуры
